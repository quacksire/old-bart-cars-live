export type SiteConfig = typeof siteConfig;

export const siteConfig = {
	name: "Old Bart Cars Live",
	description: "Track all legacy BART cars supposedly still in service after their retirement.",
};
